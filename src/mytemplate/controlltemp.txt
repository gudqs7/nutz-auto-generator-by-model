package {{basePackage}}.web;

import com.hioseo.base.BaseController;
import com.hioseo.common.MapBean;
import com.hioseo.common.PageEntity;
import com.hioseo.common.PageParams;
import {{basePackage}}.model.{{className}};
import {{basePackage}}.service.{{serviceName}};
import com.hioseo.utils.UUIDUtils;
import io.swagger.annotations.Api;
import org.springframework.web.bind.annotation.*;

import javax.annotation.Resource;
import java.util.List;
import java.util.Map;


/**
 * @author generator-wq
 * @date 2018/10/2
 */
@RestController
@RequestMapping("/api/{{controllerUrl}}")
@Api(tags = "{{controllerName}}")
public class {{controllerName}} extends BaseController {

    @Resource
    private {{serviceName}} {{serviceBeanName}};

    @GetMapping("/findAll")
    public MapBean findAll() throws Exception {
        List<{{className}}> {{className}}List = {{serviceBeanName}}.findAll();
        return success({{className}}List);
    }

}